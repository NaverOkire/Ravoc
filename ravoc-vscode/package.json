{
  "name": "ravoc-vscode",
  "displayName": "ravoc-vscode",
  "description": "RAVOC context collector and chat interface",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.120.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ravoc.openPanel",
        "title": "RAVOC: Abrir painel"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "ravoc-sidebar",
          "title": "RAVOC",
          "icon": "media/ravoc-icon.svg"
        }
      ]
    },
    "views": {
      "ravoc-sidebar": [
        {
          "type": "webview",
          "id": "ravoc.chatPanel",
          "name": "Chat",
          "icon": "media/ravoc-icon.svg"
        }
      ]
    },
    "configuration": {
      "title": "RAVOC",
      "properties": {
        "ravoc.backend.url": {
          "type": "string",
          "default": "http://localhost:7000",
          "description": "URL do backend RAVOC."
        },
        "ravoc.provider.default": {
          "type": "string",
          "default": "local_lm_studio",
          "enum": [
            "local_lm_studio",
            "openai",
            "anthropic",
            "gemini",
            "nvidia"
          ],
          "description": "Provider de LLM usado pelo chat."
        },
        "ravoc.model.default": {
          "type": "string",
          "default": "qwen2.5-coder-7b-instruct",
          "description": "Modelo padrao enviado ao provider selecionado."
        },
        "ravoc.cloud.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Habilita envio explicito de contexto para providers de nuvem."
        },
        "ravoc.lmUrl": {
          "type": "string",
          "default": "http://localhost:1234/v1/chat/completions",
          "description": "Endpoint local OpenAI-compatible usado pelo provider local."
        },
        "ravoc.lmModel": {
          "type": "string",
          "default": "qwen2.5-coder-7b-instruct",
          "description": "Modelo local legado usado por LM Studio."
        }
      }
    }
  },
  "scripts": {
    "build:webview": "npm --prefix webview install && npm --prefix webview run build",
    "vscode:prepublish": "npm run build:webview && npm run package",
    "compile": "npm run build:webview && npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm run build:webview && npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.19",
    "@types/vscode": "^1.120.0",
    "@types/ws": "^8.18.1",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1",
    "ws": "^8.20.1"
  }
}
